const {convert } = require("./csvtojson");
const sendMail = require("./sendMail");

const start = async () => {
    console.log("Starting");
    // await convert();

    
    // await sendMail();
    }

start();